"""Placeholder code location for the bronze-media orchestration spike.

Replace this empty definition set with the bounded spike workflow. Keeping the
code location separate from the webserver and daemon makes that replacement
independent from control-plane configuration.
"""

import dagster as dg


defs = dg.Definitions()
