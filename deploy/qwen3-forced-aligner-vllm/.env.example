# Copy to .env on the GPU host and edit only machine-local values.

MODEL_ID=Qwen/Qwen3-ForcedAligner-0.6B
SERVER_PORT=8000

# vLLM does not publish the optional audio dependencies in its OpenAI image.
# This deployment builds a tiny derived image that installs vllm[audio] at the
# matching version. Keep the base tag and audio extra version in lockstep.
VLLM_BASE_IMAGE=vllm/vllm-openai:v0.24.0
VLLM_AUDIO_EXTRA_VERSION=0.24.0
VLLM_AUDIO_IMAGE=qwen3-forced-aligner-vllm:0.24.0-audio

# Optional. Required for gated/private model downloads.
HF_TOKEN=
HF_HOME=/var/lib/qwen3-forced-aligner/huggingface

# Leave scheduler limits empty until running the compact-response sweep. These
# are vLLM scheduler controls, not values derived directly from VRAM size.
MAX_NUM_SEQS=
MAX_NUM_BATCHED_TOKENS=
GPU_MEMORY_UTILIZATION=

# Read-only Bronze access lets the adapter cache the compressed canonical audio
# once and decode each requested crop beside vLLM.
ALIGNER_BRONZE_ENDPOINT_URL=http://replace-with-bronze-endpoint
ALIGNER_BRONZE_BUCKET=ja-media-prod
ALIGNER_BRONZE_PREFIX=audio/anime/bronze-v2
ALIGNER_BRONZE_ADDRESSING_STYLE=path
ALIGNER_BRONZE_ACCESS_KEY_ID=
ALIGNER_BRONZE_SECRET_ACCESS_KEY=
ALIGNER_AUDIO_CACHE=/var/lib/qwen3-forced-aligner/audio
ALIGNER_ADAPTER_IMAGE=qwen3-forced-aligner-adapter:local

# Extra args are appended verbatim by scripts/run-docker.sh.
EXTRA_VLLM_ARGS=
