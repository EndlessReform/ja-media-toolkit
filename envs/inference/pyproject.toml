[project]
name = "ja-media-inference"
version = "0.1.0"
description = "Client-side inference adapters for ja-media-toolkit"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.43.78",
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "huggingface-hub>=1.21.0",
    "ja-media-core",
    "nagisa>=0.2.12",
    "numpy>=2.4.0",
    "transformers>=5.12.1",
    "uvicorn>=0.52.4",
]

[project.scripts]
qwen3-align-smoke = "ja_media_inference.qwen3_align_smoke:main"
qwen3-retime-case = "ja_media_inference.qwen3_retime_case:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
ja-media-core = { path = "../../packages/core", editable = true }

[dependency-groups]
dev = [
    "pytest>=9.1.0",
]
