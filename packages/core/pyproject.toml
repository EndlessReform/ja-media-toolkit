[project]
name = "ja-media-core"
version = "0.1.0"
description = "Shared types, config, manifests, job/result contracts"
requires-python = ">=3.13"
dependencies = [
    "numpy",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.1",
    "soundfile>=0.13.1",
]
