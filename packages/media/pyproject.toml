[project]
name = "ja-media-media"
version = "0.1.0"
description = "Filesystem/media utilities: renaming, probing, chunks"
requires-python = ">=3.13"
dependencies = [
    "ja-media-core",
]

[project.optional-dependencies]
playback = [
    "numpy>=2.4.4",
    "sounddevice>=0.5.5",
]

[tool.uv.sources]
ja-media-core = { workspace = true }
