[project]
name = "ja-media-transcripts"
version = "0.1.0"
description = "SRT/ASS parsing, normalization, alignment, scoring"
requires-python = ">=3.13"
dependencies = [
    "ja-media-core",
]

[tool.uv.sources]
ja-media-core = { workspace = true }
